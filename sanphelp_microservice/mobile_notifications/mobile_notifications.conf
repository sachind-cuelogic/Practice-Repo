upstream mobile_notifications_server {
  server 0.0.0.0:8000;
}

server {

    listen   80;
    server_name $hostname;

    client_max_body_size 4G;

    # access_log /mobile_notifications/logs/nginx-access.log;
    # error_log /mobile_notifications/logs/nginx-error.log;
 
    location / {
        proxy_pass  http://mobile_notifications_server; 
        proxy_redirect off;
        proxy_set_header Host $http_host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Host $server_name;
        add_header X-Upstream $upstream_addr;
    }

    location /static {
        alias /static; 
    }
}
